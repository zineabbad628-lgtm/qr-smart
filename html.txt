<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصة التوجيه الذكي</title>
    <style>
        body {
            font-family: sans-serif;
            background-color: #f4f7f6;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            text-align: center;
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            max-width: 400px;
            width: 100%;
        }
        h1 { color: #2c3e50; font-size: 24px; margin-bottom: 10px; }
        p { color: #7f8c8d; font-size: 16px; margin-bottom: 30px; }
        .btn {
            display: block;
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            background-color: #3498db;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            font-weight: bold;
            box-sizing: border-box;
        }
        .btn:hover { background-color: #2980b9; }
        .btn.secondary { background-color: #2ecc71; }
        .btn.accent { background-color: #e67e22; }
    </style>
</head>
<body>

<div class="container" id="app">
    <h1>مرحباً بك في منصتنا</h1>
    <p>يرجى اختيار الوجهة التي تريد الانتقال إليها:</p>
    
    <a href="https://www.google.com" class="btn">الصفحة الأولى (Google)</a>
    <a href="https://www.youtube.com" class="btn secondary">الصفحة الثانية (YouTube)</a>
    <a href="https://www.facebook.com" class="btn accent">الصفحة الثالثة (Facebook)</a>
</div>

<script>
    // الروابط الذكية للتحويل التلقائي
    const pages = {
        page1: "https://www.google.com",
        page2: "https://www.youtube.com",
        page3: "https://www.facebook.com"
    };

    // قراءة الرابط
    const urlParams = new URLSearchParams(window.location.search);
    const target = urlParams.get('to');

    // إذا وجد كلمة ?to= يوجّه تلقائياً
    if (target && pages[target]) {
        document.getElementById('app').innerHTML = `
            <div style="font-size: 18px; color: #3498db; font-weight: bold;">جاري تحويلك تلقائياً...</div>
            <p>إذا لم يتم تحويلك، <a href="${pages[target]}">اضغط هنا</a>.</p>
        `;
        window.location.href = pages[target];
    }
</script>
</body>
</html>
